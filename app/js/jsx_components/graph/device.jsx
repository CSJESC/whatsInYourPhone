"use strict";

var React = require('react');

var Page = React.createClass({

  render: function () {
    return (
      <div 
        className = "device">
        device
      </div>
    );
  }
});

module.exports = Page;